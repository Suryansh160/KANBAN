# Used by the deployed frontend when the API/socket server has a different URL.
# The value must be the server origin, with no /api suffix or trailing slash.
VITE_SOCKET_URL=https://kanban-muew.onrender.com
